// import PropTypes from 'prop-types';

import { Box, Typography } from '@mui/material';

function WaiterView() {
  return (
    <Box>
      <Typography>Waiter view</Typography>
    </Box>
  );
}
export default WaiterView;
// WaiterView.propTypes = { tables: PropTypes.array };
